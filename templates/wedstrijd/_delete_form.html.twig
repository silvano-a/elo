<form method="post" action="{{ path('wedstrijd_delete', {'id': wedstrijd.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wedstrijd.id) }}">
    <button class="btn">Delete</button>
</form>
