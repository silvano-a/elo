{% extends 'base.html.twig' %}

{% block title %}Wedstrijd{% endblock %}

{% block body %}
    <h1>Wedstrijd</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wedstrijd.id }}</td>
            </tr>
            <tr>
                <th>Timestamp</th>
                <td>{{ wedstrijd.timestamp ? wedstrijd.timestamp|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('wedstrijd_index') }}">back to list</a>

    <a href="{{ path('wedstrijd_edit', {'id': wedstrijd.id}) }}">edit</a>

    {{ include('wedstrijd/_delete_form.html.twig') }}
{% endblock %}
